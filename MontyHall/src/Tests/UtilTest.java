package Tests;

import org.junit.jupiter.api.Test;

class UtilTest {

    /**
     * La méthode poserQuestionString() utilise un scanner, elle n'est donc pas éligible à être testée.
     * Cette méthode devrait normalement tester la méthode poserQuestionString(), qui pose une question dont la réponse est une chaîne de caractères.
     */
    @Test
    void poserQuestionString() {
    }

    /**
     * La méthode poserQuestionInt() utilise un scanner, elle n'est donc pas éligible à être testée.
     * Cette méthode devrait normalement tester la méthode poserQuestionInt(), qui pose une question dont la réponse est un entier.
     */
    @Test
    void poserQuestionInt() {
    }
}